{
  "name": "cov_playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "allure:open": "npx allure serve allure-results",
    "test:report": "npx playwright test --reporter=line,allure-playwright && npx allure serve allure-results",
    "test:auto": "npx playwright test && npx allure serve allure-results",
    "test:windows": "powershell -ExecutionPolicy Bypass -File ./test-and-report.ps1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "allure-commandline": "^2.36.0",
    "allure-playwright": "^3.4.5"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "winston": "^3.19.0"
  }
}